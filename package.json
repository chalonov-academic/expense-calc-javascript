{
  "name": "expense-calculator-tests",
  "version": "1.0.0",
  "description": "Tests for expense calculator app",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "collectCoverage": false,
    "verbose": true
  }
}